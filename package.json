{
  "name": "arbeidsplassen-playwright",
  "version": "1.0.0",
  "description": "End-to-end tests for Arbeidsplassen using Playwright",
  "type": "module",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
