{
  "name": "arbeidsplassen-playwright",
  "version": "1.0.0",
  "description": "End-to-end tests for Arbeidsplassen using Playwright",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:dev-e2e": "playwright test dev-e2e",
    "test:prod-uptest": "playwright test prod-uptest",
    "test:localhost": "LOCALHOST=true playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
